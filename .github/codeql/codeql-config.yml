# CodeQL config: exclude paths that cause "Unterminated comment" / parse errors.
# (e.g. node_modules, VitePress docs, vendor)
name: "Native Content Relationships"
paths-ignore:
  - node_modules
  - '**/node_modules/**'
  - docs
  - '**/docs/**'
  - vendor
  - '**/vendor/**'
  - '**/*.min.js'
